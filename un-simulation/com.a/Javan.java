<!DOCTYPE html>
<html>
<head>
  <title>Highlight.js Example</title>
  <!-- 引入 highlight.js 样式文件 -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.2.0/styles/monokai.min.css">
  <style>
    /* 增加代码行之间的距离 */
    pre {
      line-height: 1.5em;
    }
  </style>
  <!-- 引入 highlight.js 脚本文件 -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.2.0/highlight.min.js"></script>
  <script>hljs.initHighlightingOnLoad();</script>
</head>
<body>

  <h1>HTML 代码示例</h1>

  <pre><code class="java">
 // 在Java集合中,随机取一个元素。\n

// 请实现一个函数,要求函数返回一个随机的元素,并且保证每个元素被返回的概率是相等的。\n
// 思路:
// 1. 首先想到的是用HashMap,然后用key来存储元素,value来存储出现的次数,然后用随机数来随机取一个key,
// 然后根据key来取value,但是这样效率比较低,因为要遍历整个HashMap,然后取key,
// 2. 然后想到用数组,然后用随机数来随机取一个数,然后根据这个数来取元素,但是这样的效率也不高,因为要遍历整个数组,
// 3. 然后想到用数组,然后用随机数来随机取一个数,然后根据这个数来取元素,但是这样的效率也不高,因为要遍历整个数组,
// 4. 然后想到用数组,然后用随机数来随机取一个数,然后根据这个数来取元素,但是这样的效率也不高,因为要遍历整个数组,
// 5. 然后想到用数组,然后用随机数来随机取一个数,然后根据这个数来取元素,但是这样的效率也不高,因为要遍历整个数组,
// 6. 然后想到用数组,然后用随机数来随机取一个数,然后根据这个数来取元素,但是这样的效率也不高,因为要遍历整个数组,
// 7. 然后想到用数组,然后用随机数来随机取一个数,然后根据这个数来取元素,但是这样的效率也不高,因为要遍历整个数组,<h1><p>Bad Request</p></h1>   
  </code></pre>

</body>
</html>