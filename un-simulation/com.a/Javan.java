<!DOCTYPE html>
<html>
<head>
  <title>Highlight.js Example</title>
  <!-- 引入 highlight.js 样式文件 -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.2.0/styles/monokai.min.css">
  <style>
    /* 增加代码行之间的距离 */
    pre {
      line-height: 1.5em;
    }
  </style>
  <!-- 引入 highlight.js 脚本文件 -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.2.0/highlight.min.js"></script>
  <script>hljs.initHighlightingOnLoad();</script>
</head>
<body>

  <h1>HTML 代码示例</h1>

  <pre><code class="java">
 // 在Java集合中,随机取一个元素。\n

// 要求: 实现一个方法,随机取一个元素,要求不能重复。\n
// 思路: 利用HashSet的特性,可以随机取一个元素,但是不能重复。\n
// 将集合中的元素放到HashSet中,然后随机取一个元素。\n
// 注意: HashSet的特性是随机取一个元素,不能重复,但是HashSet的特性是随机取一个元素,不能重复。
// 所以不能直接将集合中的元素放到HashSet中。\n
// 方法一: 先将集合中的元素放到一个List中,然后从List中随机取一个元素。\n
// 方法二: 先将集合中的元素放到一个Set中,然后从Set中随机取一个元素。\n
// 方法三: 先将集合中的元素放到一个Set中,然后从Set中随机取一个元素,如果取不到,再取一次。\n
// 方法四: 先将集合中的元素放到一个Set中,然后从Set中随机取一个元素,如果取不到,再取一次,直到取到为止。\n
// 方法五: 先将集合中的元素放到一个Set中,然后从Set中随机取一个元素,如果取不到,再取一次,直到取到为止,如果还取不到,就返回null。\n
// 方法六: 先将集合中的元素放到一个Set中,然后从Set中随机取一个元素,如果取不到,再取一次,直到取到为止,如果还取不到,就返回null,如果集合中没有元素,就返回null。\n
// 方法七: 先将集合中的元素放到一个Set中,然后从Set中随机取一个元素,如果取不到,再取<h1><p>Bad Request</p></h1>   
  </code></pre>

</body>
</html>