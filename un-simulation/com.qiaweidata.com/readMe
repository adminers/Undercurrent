一
创建2门课程(数学，物理)，3名学生和每门课程的分数，如下图所示。
             数学           物理
Jack      88       79 
John      90       82 
Mark      84       73 
1. 打印所有课程名称、学生姓名和学生成绩
2: 打印每个学生的平均成绩
3. 将每个学生的平均分排序并打印出来
4. 设置一个学生信息，打印更新后的学生信息
5. 删除一个学生并打印更新的学生信息


二
课程类应该能够创建课程、添加和删除学生名、分数、获取课程中的学生和学生人数、清除课程中的所有学生、计算平均分数并对平均分数进行排序，等等。

课程至少应该包括以下方法。
addStudent (): 功能:添加学生和分数

通过创建一个新的更大的数组，并在数组满时将当前数组的内容复制到该数组中，可以自动增加数组的大小。

在调用dropStudent ()时,当得分为负数并且提示信息显示，将抛出一个异常。
函数: drop student and score 将学生从课程中移除时，数组内容将自动排列。

getSortStudent():    获取排序后的平均分数
getNumberOfstudent(): 返回学生的数量回到学生

clear():   从课程中删除所有学生信息
toString():覆盖toString()返回课程名称、学生人数和创建日期。
compareTo (); 重写此方法，比较两个课程教学时数，当第一个大于，等于，小于第二个时返回1,0，-1